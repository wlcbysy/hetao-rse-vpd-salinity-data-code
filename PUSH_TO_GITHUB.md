# GitHub Repository Record

This data and code package has been uploaded to the public GitHub repository:

https://github.com/wlcbysy/hetao-rse-vpd-salinity-data-code

The local repository is configured with:

```bash
git remote -v
git status --short
git log --oneline -1
```

Use `git push` from this folder after future package updates.
